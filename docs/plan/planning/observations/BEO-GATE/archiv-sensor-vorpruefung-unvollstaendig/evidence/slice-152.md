**Vorgang:** slice-152
**Fund:** Dritter Beleg. Zwei neue Ausprägungen: (1) derselbe Titel-Link-Bug wie in slice-148 (`ApplyReview`) fand sich unverändert in `Apply` (Wellen-Modus) und `ApplySlice` (wellenlos) — der Fix an einer Stelle zog nicht automatisch an den beiden strukturell identischen anderen nach. (2) Ein vollständig geleertes `docs/plan/planning/done/` (keine flache Slice-Datei mehr) bricht zwei fail-closed-Sensoren (`verify-risiko-ausgaenge`, `.d-check.yml`-Modul `structure`), die „mindestens eine Datei" stillschweigend voraussetzen — nicht behoben, sondern durch einen belassenen Slice-Puffer umgangen (Präzedenz: d-check hält ebenfalls nie exakt null). Bei 3× wird die Beobachtung gemäß Register-Regel zur verkörperten Regel: Invarianten-Kommentar in `tools/archive-wave/archive.go` + je ein Regressionstest pro Stub-Erzeugungsfunktion.
