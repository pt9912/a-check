**Stand:** offen (1×)

Kein Sensor deckt die Attestierung in `in-progress/`. Der Sensor `doc-reviews` prüft
absichtlich nur `done/` — sein Geltungsbereich ist begründet und wird hier nicht bestritten;
die Beobachtung benennt, was daneben ungeprüft bleibt.
