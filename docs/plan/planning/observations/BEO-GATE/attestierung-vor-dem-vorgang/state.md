**Stand:** offen (1×)

Kein Sensor deckt eine Attestierung, solange der Slice in `in-progress/` liegt. Der
Geltungsbereich von `make doc-reviews` (nur `done/`) ist begründet und wird hier nicht
bestritten; die Beobachtung benennt, was daneben ungeprüft bleibt.
