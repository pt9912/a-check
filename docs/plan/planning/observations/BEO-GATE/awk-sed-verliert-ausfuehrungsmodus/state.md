**Stand:** offen

beide Male fiel es nur an der `mode change`-Zeile des Commits auf und kostete einen Reparatur-Commit. Der Modus ist hier keine Zusage (die Skripte laufen über `bash`), aber eine Datei soll tragen, was sie vorher trug. Abhilfe wäre ein `chmod +x` nach jedem Neuschreiben — eine Disziplin, kein Sensor
