**Vorgang:** slice-135
**Fund:** Die neue Baseline-Regel „ein Beleg-Lauf darf nicht vom Gate-Target abhängen, eine Fehler-Toleranz gehört nie an einen Gate-Lauf" wurde nur überflogen (22 Fundstellen in `tools/*.sh`/`Makefile`), nicht Zeile für Zeile gegen den eigenen Bestand geprüft
