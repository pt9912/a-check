# Gate/Beleg-Trennung nur ueberflogen

**Sub-Area:** Gate-/Werkzeug-Schicht
**Ehemals:** `BEO-034` (Tabellenform, bis slice-139)

Die neue Baseline-Regel „ein Beleg-Lauf darf nicht vom Gate-Target abhängen, eine Fehler-Toleranz gehört nie an einen Gate-Lauf" wurde nur überflogen (22 Fundstellen in `tools/*.sh`/`Makefile`), nicht Zeile für Zeile gegen den eigenen Bestand geprüft
