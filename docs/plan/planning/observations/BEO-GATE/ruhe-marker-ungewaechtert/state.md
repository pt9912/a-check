**Stand:** verkörpert

**verkörpert** in [`.d-check.yml`](../../../../../../.d-check.yml) §`planning` (seit slice-122), Target im `gates`-Aggregat. Gemessen: die Defaults melden **fail-closed**, nicht falsch — die Lücke war gar keine Konfiguration, nicht eine schlechte. Der zweite Fall (`doc-structure` ohne Block) war mit slice-080 entfallen
