**Stand:** verkörpert

**verkörpert für die Risiko-Ausgänge** in `tools/verify-risiko-ausgaenge.sh` (seit slice-129): geprüft wird `in-progress/` mit, sobald die Closure-Notiz **ausgefüllt** ist — der Auslöser ist ihr Zustand, nicht das Verzeichnis. **Offen bleibt die andere Hälfte:** `doc-structure` (Closure-Struktur) prüft weiter nur `done/`; sein `files`-Glob kann „nur wenn ausgefüllt" nicht ausdrücken, ein Glob über `in-progress/` beanstandete jeden laufenden Slice
