**Stand:** offen (2×)

Unterhalb der Schwelle. Die beiden Vorkommen liegen in zwei aufeinanderfolgenden Vorgängen
(slice-187, slice-188) und betreffen denselben Gegenstand: den `git mv` des Lifecycle-Übergangs.
Beide sind **nicht** rückwirkend korrigierbar — ein Commit-Betreff steht in `git`.

Was trägt, ist die Frage beim Schreiben eines Lifecycle-Commits: *Nennt der Betreff das Werkzeug
für etwas, das der Lauf getan hat?* Beim dritten Vorkommen wäre die naheliegende Antwort eine
**Vorlage** für diesen Betreff statt einer weiteren Notiz.
