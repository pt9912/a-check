**Vorgang:** slice-185

**Fund:** Die Tabellenzeile `Makefile` schloss mit *„alle sechs sind Bootstrap-Bedienhinweise"*.
Der Kopfkommentar der Ziel-Form trägt daneben einen Satz mit Norm-Charakter (*„nur behauptete
Targets in `AGENTS.md` / `harness/README.md` eintragen — keine halluzinierten Gates"*). Der
Ausgang *ohne Befund* war richtig — a-check trägt die Norm an beiden Stellen —, die Begründung
deckte den Kandidaten nicht.

Gefunden hat es der unabhängige Review (Report zu slice-185, F-11, `klasse`:
*Kandidaten-Klassifikation gröber als der Kandidat*).

**Nachgetragen mit slice-186**, als dieselbe Klasse ein zweites Mal auftrat. Die Kennung entstand
dort; dieser Beleg ist das Erstauftreten.
