**Vorgang:** slice-176
**Fund:** Dritter Vorgang in Folge, in dem `make slice-mv` den Verweis in der **flach** liegenden
Welle-Datei stehen ließ — `[slice-176](open/…)` mit Verzeichnis-Präfix, aber ohne `../`. Die
Commit-Message des Lifecycle-Wechsels nennt es selbst; `make doc-check` meldete es, die Korrektur
war ein `sed`.

**Die Vorhersage aus `evidence/slice-175.md` ist damit für Etappe C eingetroffen** — dort stand,
bei den verbleibenden Etappen kämen je zwei Vorkommen dazu. Nach diesem Slice sind es **fünf
Vorkommen in drei Vorgängen**; für die Etappen D und E stehen vier weitere aus.

**Warum der Eintrag trotzdem auf *verkörpert* bleibt:** Die Verkörperung fängt die Formen, für die
sie gebaut wurde. Was fehlt, ist eine dritte, die a-check vor `welle-15` nicht hatte — eine offene
Welle-Datei, die auf Slices in Lifecycle-Verzeichnissen zeigt. Bei fünf Vorkommen und vier
absehbaren ist die Frage nicht mehr, *ob* das Werkzeug sie lernen sollte, sondern wann jemand die
zehn Zeilen schreibt. Aufgenommen als Kandidat für die Welle-Closure.
