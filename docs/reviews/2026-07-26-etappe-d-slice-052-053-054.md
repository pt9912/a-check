# Review-Report: Etappe D — slice-052 + slice-053 + slice-054 — 2026-07-26

**Review-Art:** Code-Review — geprüft gegen die Slice-Pläne, `AGENTS.md` §3/§4/§5, Regelwerk
`modul-05` (Slice-Form, Lifecycle), `modul-06` (Roadmap-Abschnitte), `modul-03` (AC-Form) und
`modul-13` (bootstrap-aware Gate).

**Unabhängigkeit — ausdrücklich:** **Selbst-Review**, kein unabhängiger Lauf (neues
Kontextfenster, dieselbe Modell-Familie wie die Autoren-Instanz).

**Gegenstand:** `71b8844..51d5999` (12 Commits) — 691 eingefügte Zeilen in zehn Dateien, darunter
zwei neue Sensoren.

**Skill:** `.harness/skills/reviewer.md` @ `20ee992` (2026-07-25) · <!-- d-check:ignore (Adopter-spezifischer Skill-Pfad, existiert im Ziel-Repo ggf. nicht) -->
**Modell:** `claude-opus-5[1m]` · **Datum:** 2026-07-26

**Eingangs-Kontext:**

- [slice-052](../plan/planning/done/slice-052-slice-form.md),
  [slice-053](../plan/planning/done/slice-053-lifecycle-und-roadmap-form.md),
  [slice-054](../plan/planning/done/slice-054-ac-form.md) (Gegenstand)
- [slice-048 §5](../plan/planning/done/slice-048-modul-delta-lesen.md) — Funde B-1, B-5, B-6,
  B-12, B-15, B-18
- [`AGENTS.md`](../../AGENTS.md) §5 (Lifecycle, WIP-Limit, AC-Form, Slice-Form), §4
- [`harness/conventions.md` §Anforderungs-Anlege-Prozess](../../harness/conventions.md#anforderungs-anlege-prozess)
- `docs/plan/planning/slice.template.md`,
  [`next/README.md`](../plan/planning/next/README.md)
- frühere Findings: [Etappe E](2026-07-26-etappe-e-slice-050-051.md) F-1 (gleiches Muster),
  [slice-049](2026-07-26-slice-049-mechanik-sensoren.md) F-2

---

## Findings

### F-1 — Das Etiketten-Muster wiederholt sich: „Link-Fix" liefert den halben Folge-Slice

- `kategorie`: **HIGH**
- `quelle`: Klasse „nachweislich falsche Tatsachenbehauptung / Harness-Lüge"
  ([Reviewer-Skill](../../.harness/skills/reviewer.md) §HIGH); [`AGENTS.md`](../../AGENTS.md) §5,
  §3.3 (Begründung: die Historie muss lesbar bleiben)
- `pfad`: Commits `d436da9` und `4f9fa5c`
- `befund`: `d436da9` trägt den Betreff **„docs(planning): slice-052 Folge-Slice-Link auf den
  Ziel-Pfad"** und ändert **115 Zeilen in vier Dateien**: `AGENTS.md` (+20 — der vollständige
  Lifecycle mit beiden Rückführungen **und** die Deklaration „WIP-Limit = 1", also Fund B-6),
  `docs/plan/planning/next/README.md` (neu, 29 Zeilen — Fund B-18) und das Plandokument
  `slice-053-lifecycle-und-roadmap-form.md` (neu, 67 Zeilen). Der tatsächliche Link-Fix umfasst
  2 Zeilen. Der zugehörige `feat`-Commit `4f9fa5c` — Betreff **„feat(harness): Lifecycle
  vollstaendig, next/ wiederhergestellt, Drift-Log (slice-053, slice-048)"** — liefert von den drei
  genannten Dingen nur das **Drift-Log** (21 Zeilen Roadmap); „Lifecycle vollständig" und „next/
  wiederhergestellt" waren zu diesem Zeitpunkt bereits im vorangegangenen „Link-Fix" enthalten.
- `verifizierbar`: **ja — belegt.** `git show --stat d436da9` → `4 files changed, 115 insertions(+)`
  mit `AGENTS.md` und `next/README.md`; `git show --stat 4f9fa5c` → `2 files changed, 49
  insertions(+)`, nur `roadmap.md` und das eigene Plandokument.
- `gegenprobe`: Geprüft, ob es sich um denselben Commit-Paar-Fehler wie in Etappe E handelt oder um
  eine andere Ursache — es ist strukturell identisch (ein `fix`/`docs`-Commit trägt die Substanz,
  der zugehörige `feat`-Commit trägt den Namen). Damit liegt **die dritte** Instanz vor:
  `615e37f` (408 Zeilen), `f57289d` (200), `d436da9` (115).

### F-2 — Drittes gleichartiges Vorkommen, kein Steering-Loop-Eintrag

- `kategorie`: **MEDIUM**
- `quelle`: [`AGENTS.md`](../../AGENTS.md) §5 §Steering-Loop („ab dem **zweiten** gleichartigen
  Vorfall entsteht ein Eintrag, ab dem **dritten** ist es eine Harness-Lücke und verlangt einen
  Guide oder Sensor")
- `pfad`: [`docs/plan/steering-loop.md`](../plan/steering-loop.md)
- `befund`: Der Fehlertyp „Commit-Betreff und Traceability-ID bezeichnen nicht die im Commit
  enthaltene Arbeit" tritt in dieser Kette dreimal auf (`615e37f`, `f57289d`, `d436da9`) und
  einmal spiegelbildlich als `feat`-Commit ohne die benannte Substanz (`0f868d7`, `4f9fa5c`). Der
  Steering-Loop führt `SL-001` (Pipe) und `SL-002` (Verweise nach `git mv`), aber keinen Eintrag
  für dieses Muster — obwohl es nach der eigenen Regel oberhalb der 3×-Schwelle liegt und damit
  einen Guide oder Sensor verlangt.
- `verifizierbar`: ja — Abgleich Betreff ↔ `--stat` über alle 36 Commits der Kette; drei
  Ausreißer.

### F-3 — `verify-slice-form` zählt Checkboxen dateiweit statt im DoD-Abschnitt

- `kategorie`: **LOW**
- `quelle`: Maintainability; Konsistenz mit den Schwester-Sensoren
- `pfad`: [`tools/verify-slice-form.sh:45`](../../tools/verify-slice-form.sh)
- `befund`: Der Kommentar deklariert die Regel als „(1) hoechstens 3 **DoD**-Punkte"; die
  Implementierung zählt mit `grep -cE '^- \[[ x]\] ' "$f"` **jede** Checkbox-Zeile der Datei,
  unabhängig vom Abschnitt. Ein künftiger Slice mit einer Checkliste außerhalb des DoD-Abschnitts
  (etwa in „Was offen bleibt") würde damit fälschlich als zu groß gemeldet. Die beiden
  Schwester-Sensoren schneiden sauber: `verify-closure-notes.sh` und `verify-ac-form.sh` grenzen
  den geprüften Abschnitt jeweils per `awk` ab.
- `verifizierbar`: **ja — geprüft.** Über `slice-052` … `slice-057` stimmen dateiweite und
  DoD-abschnittsweise Zählung heute in **allen** Fällen überein; der Defekt ist latent, nicht
  aktiv. Er ist zudem fail-closed (Fehlalarm blockiert, verliert nichts).

### F-4 — `make verify` maskiert Folgebefunde

- `kategorie`: **LOW**
- `quelle`: `modul-11` (die Verifikations-Schicht beantwortet mehrere unabhängige Fragen)
- `pfad`: [`Makefile`](../../Makefile), Ziel
  `verify: verify-closure-notes verify-slice-form verify-ac-form`
- `befund`: Die drei Teil-Sensoren hängen als sequenzielle Prerequisites; `make` bricht beim
  ersten roten Target ab. Wer `make verify` mit Verstößen in zwei Bereichen laufen lässt, sieht
  nur den ersten und hält den Rest für grün, bis er erneut läuft. Kein False-Green (grün wird es
  nie, solange etwas rot ist), aber eine unvollständige Diagnose ausgerechnet in der Schicht, die
  vor der „fertig"-Meldung Auskunft geben soll.
- `verifizierbar`: **ja — belegt.** Mit einem Slice-Form- **und** einem AC-Form-Verstoß zugleich
  meldete `make verify` (EXIT=2) ausschließlich die beiden Slice-Form-Befunde; die vier
  AC-Form-Befunde wurden erst im isolierten `make verify-ac-form` sichtbar.

## Negativbefunde

- geprüft, ohne Befund: **`verify-slice-form` greift wirklich** — **Negativ-Probe gefahren**, nicht
  geglaubt: ein neu angelegter Slice mit **4** DoD-Punkten und einer Closure-Notiz ohne benannte
  Lerneintrag-Form ergab beide erwarteten Befunde und `EXIT=2`
  (`4 DoD-Punkte — hoechstens 3 erlaubt … zerlegen statt dehnen` /
  `Closure-Notiz benennt keine der drei Lerneintrag-Formen`).
- geprüft, ohne Befund: **`verify-ac-form` greift wirklich** — **Negativ-Probe gefahren**: eine neu
  ins Lastenheft eingefügte `AC-FA-PROBE-001` ohne die drei Pfade ergab vier Befunde und `EXIT=2`.
  Der Sensor ist damit belegt wirksam, obwohl er im Bestand aktuell **0** AC prüft (alle 19 sind
  grandfathered) — seine Wirksamkeit ruht also auf Selbsttest und dieser Probe, nicht auf einem
  realen Fund.
- geprüft, ohne Befund: **Grandfathering-Mechanik (AC)** — die Liste ist **namentlich**, nicht
  heuristisch; sie enthält exakt die behaupteten **19** IDs (nachgezählt), und der Selbsttest prüft
  **beide** Richtungen (Bestands-AC muss grandfathered sein, `AC-FA-NEU-001` darf es nicht sein).
  Damit trägt die Aussage „die Liste wächst nicht mit" — eine Datums- oder Nummern-Heuristik hätte
  diese Eigenschaft nicht.
- geprüft, ohne Befund: **Grandfathering-Mechanik (Slice)** — `SLICE_FORM_FROM=52` ist als
  **Stichtag** dokumentiert, ausdrücklich mit Hochschalt-Trigger („die Schwelle wandert NICHT
  weiter … sie fällt weg, wenn alle Slices unterhalb ebenfalls konform sind"). Das schließt das in
  `modul-13` benannte „Bootstrap-Schlupfloch". Die Behauptung „51 ältere Slices" ist exakt
  (nachgezählt).
- geprüft, ohne Befund: **Verifikations-Lauf auf der Wegmarke** — `make verify` auf `51d5999`:
  **EXIT=0** mit `51 Slice(s) in done/`, `3 Slice(s) ab slice-52 geprueft, 51 aelter`,
  `0 neue AC(s) geprueft, 19 … grandfathered`. Alle drei Zählungen decken sich mit den
  Plan-Behauptungen.
- geprüft, ohne Befund: **Doku-Gate auf der Wegmarke** — `make doc-check` auf `51d5999`: EXIT=0.
- geprüft, ohne Befund: **ehrliche Grenzen beider Sensoren** — `verify-slice-form` nimmt „höchstens
  zwei Schichten" ausdrücklich aus (Ermessensfrage über Modul-Grenzen, „ein Zähler darüber wäre
  Schein-Genauigkeit"), `verify-ac-form` nimmt die Frage aus, ob hinter „Boundary:" wirklich ein
  Randfall steht. Beide Grenzen sind im Skript benannt statt stillschweigend gezogen.
- geprüft, ohne Befund: **B-18 (`next/`)** — `docs/plan/planning/next/README.md` existiert und
  erklärt den Zustand, einschließlich der Klarstellung, dass `next/` ein Ort und keine
  Pflichtstation ist; `AGENTS.md` §5 nennt beide Rückführungen.
- geprüft, ohne Befund: **B-12 (Drift-Log)** — die Roadmap trägt den Abschnitt „Historische
  Trigger-Verschiebungen" mit Begründung, warum er neben dem Closure-Log steht („dort steht, *was*
  geschlossen wurde, hier *was sich unterwegs verschoben hat*").
- geprüft, ohne Befund: **AC-Bestand unangetastet** — die 19 vertraglich bindenden `AC-*` sind
  nicht umgeschrieben worden; die Etappe ändert `spec/lastenheft.md` nicht.

## Summary

| Kategorie | Anzahl |
|---|---|
| HIGH | 1 |
| MEDIUM | 1 |
| LOW | 2 |
| INFO | 0 |

## Verdikt

**Merge-blockierend: nein — mit zwei Auflagen.**

Die Substanz dieser Etappe ist die bislang beste geprüfte: beide neuen Sensoren sind **belegt
wirksam** (je eine Negativ-Probe in diesem Review gefahren, nicht aus dem Slice übernommen), das
Grandfathering ist namentlich statt heuristisch gelöst, und der Stichtag trägt einen ausdrücklichen
Hochschalt-Trigger — genau die Stelle, an der bootstrap-aware Gates sonst zum Dauer-Schlupfloch
werden. Die drei Funde B-6, B-12, B-18 sind vollständig geliefert.

F-1 ist erneut ein Buchführungs-, kein Substanzfund, und blockiert den Merge aus denselben Gründen
nicht wie in Etappe E. Neu ist F-2: mit dieser Instanz überschreitet das Muster die 3×-Schwelle
der eigenen Steering-Loop-Regel. Das ist keine Stilfrage mehr — die Regel sagt ausdrücklich, dass
„besser aufpassen" ab hier keine Antwort ist. Der Kanal existiert seit slice-057; ihm fehlt genau
dieser Eintrag.

Die beiden LOW sind Präzisionspunkte an sonst sauberen Sensoren; F-3 ist latent und fail-closed,
F-4 kostet nur Diagnose-Runden.

**Übergabe:** Findings gehen an die Implementation. Der Report ersetzt keine Verifikation —
DoD-/Spec-Konformität prüft `make verify` separat (Modul 11).
