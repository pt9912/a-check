# Review-Report: slice-171 — 2026-09-08

**Review-Art:** unabhängiger Lauf — anderer Kontext als die Implementierung, kein
`fork`; geprüft gegen Slice-Plan, Hard Rules und den Konventions-Bestand.
Kern-Behauptung des Gegenstands sind **Zahlen**: Der Slice ersetzt eine
ungemessene Begründung durch eine gemessene, also ist jede Zahl der neuen
Begründung nachzuzählen.

**Gegenstand:** Commits `015c025` („MR-011 nach conventions/done"), `9bb9cf2`
(„MR-021 loest MR-011 ab -- gemessene Begruendung") und `1f6aed7`
(„slice-171 -- Umsetzung, Risiko-Ausgaenge, Closure-Notiz"). Der
Lifecycle-Commit `eb79a60` wurde als Kontext mitgelesen.

**Skill:** `.harness/skills/reviewer.md` @ Stand `3fae6d3` · <!-- d-check:ignore -->
**Modell:** claude-opus-5[1m] · **Datum:** 2026-09-08

**Eingangs-Kontext:**

- `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md`
- `harness/conventions.md` §Adaptions-Block, `harness/conventions/MR-021-verfeinerungs-form.md`,
  `harness/conventions/done/MR-011-verfeinerungs-form.md`
- `AGENTS.md` §3 (Hard Rules), §5 (Dokumentations-Regeln), §6
- `spec/spezifikation.md`, `spec/lastenheft.md`, alle 40 Dateien unter `docs/plan/adr/`
- `docs/plan/planning/observations/BEO-HARNESS/` (alle 14 Einträge)
- `.harness/skills/reviewer.md`, `.d-check.yml`
- Baseline `v6.5.0` · `regelwerk/modul-05-planning-harness.md`,
  `regelwerk/modul-06-roadmap.md`
- eigene Läufe: `make gates` (Exit 0), `make verify` (Exit 0),
  `make commit-scope-check` (Exit 0)

**Geltungsbereich dieses Reviews:** Nachgezählt wurden **alle** Zahlen in
`MR-021` und in den §2/§3/§7/§9-Blöcken des Slice-Plans, dazu die
Anker-Auflösung, die Rename-Erkennung und der Register-Sichtungsschritt. **Nicht
geprüft:** Go-Quellen und Gate-Skripte — der Gegenstand fasst keine an; der
Inhalt der `*-archiv.zip` unter `done/`; und ob die *Entscheidung* (keine
Suffix-Form) richtig ist — sie steht laut `MR-021` ausdrücklich nicht zur
Debatte und ist in §1 des Plans ausgeschlossen.

---

## Findings

### F-1 — `MR-021` §Begründung Grund 1: die Zahl „40" trägt in keiner Lesart

- `kategorie`: HIGH
- `quelle`: Reviewer-Skill §Klassifikation, „nachweislich falsche
  Tatsachenbehauptung (gegen ein Repo-Artefakt verifiziert)"; `AGENTS.md` §5
  („Agenten referenzieren IDs, sie erfinden keine" — hier: Zahlen)
- `pfad`: `harness/conventions/MR-021-verfeinerungs-form.md:18–19` (Grund 1) und
  `:29` (Korrektur-Block); dieselbe Zahl in
  `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:173`
  (Mess-Tabelle §3), `:215` (Risiko-Ausgang §7) und `:239` (Closure-Notiz §8)
- `befund`: Der Satz lautet „Gemessen: **40** ADR-Dateien tragen ein
  `Schärft:`-Feld, das auf solche Anker zeigt" und stützt eine Aussage über
  `Accepted`-ADRs. Gemessen: `docs/plan/adr/` enthält 40 Markdown-Dateien, davon
  **39** ADRs und den Index `README.md`; der Index trägt **kein** `Schärft:`-Feld,
  sondern zwei Prosa-Erwähnungen des Worts. Von den 39 Feld-Zeilen zeigen **33**
  auf einen `SPEC-*`-Anker — sechs (ADR-0001, 0005, 0006, 0021, 0037, 0038)
  tragen `—` —, und von diesen 33 sind **29** `Accepted`; die übrigen vier
  (ADR-0012, 0020, 0022, 0031) sind `Superseded`. Damit ist keine der drei
  Größen, über die der Satz reden kann (Dateien mit Feld · Felder mit Anker ·
  `Accepted`-Felder mit Anker), gleich 40. Die Herkunft der Zahl ist im Slice-Plan
  sichtbar: §2.1 zählt **41** *Vorkommen* des Strings `**Schärft:**` im
  ADR-Verzeichnis (39 Feld-Zeilen + 2 Index-Prosa), §3 macht daraus „40
  ADR-Dateien mit Feld" — gezählt wurde das *Wort*, ausgesagt wird über *Felder,
  die zeigen*. Der Eintrag ist `Accepted` und wird nach der Adaptions-Disziplin
  nicht überschrieben.
- `verifizierbar`: ja — vier Handgriffe, alle im Repo reproduzierbar:
  `grep -roh '\*\*Schärft:\*\*' docs/plan/adr/ | wc -l` → **41**;
  `grep -rlE '^- \*\*Schärft:\*\*' docs/plan/adr/ | wc -l` → **39**;
  `grep -rlE '^- \*\*Schärft:\*\* —' docs/plan/adr/*.md | wc -l` → **6**;
  `grep -nE '^- \*\*Schärft:\*\*' docs/plan/adr/README.md` → kein Treffer.
  Kein Gate deckt das: `make gates` und `make verify` sind grün (eigene Läufe,
  Exit 0), weil kein Modul Zahlen in Prosa gegen den Bestand hält.
- `klasse`: Messung stützt nicht die Menge, über die der Satz aussagt — dieselbe
  Klasse, die der Slice bei `MR-011` behebt

### F-2 — Slice §9: „Keine weiteren Treffer für `HARNESS`" ist gegen das Register falsch

- `kategorie`: HIGH
- `quelle`: Baseline `v6.5.0` · `regelwerk/modul-05-planning-harness.md`
  §Zwei Schritte vor der Modus-Begründung („Das Beobachtungs-Register durchgehen:
  Steht eine der berührten Sub-Areas dort?"); `AGENTS.md` §5 §Beobachtungs-Register
- `pfad`: `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:301`
  („**Keine weiteren Treffer** für `HARNESS`.")
- `befund`: Der Slice führt genau eine berührte Sub-Area (`HARNESS`) und nennt im
  Sichtungs-Block zwei Einträge. Gemessen führt
  `docs/plan/planning/observations/BEO-HARNESS/` **14** Einträge — am Anlage-Tag des
  Plans (`d294a72`) ebenfalls 14 —, davon heute **10** mit Stand `offen`.
  Mindestens vier davon berühren den Gegenstand dieses Slice
  unmittelbar und sind nicht genannt:
  `mr-aufloesungs-trigger-ohne-waechter` (1×, offen — „Der Auflösungs-Trigger
  eines `MR`-Eintrags hat keinen Wächter"; der Slice legt einen `MR`-Eintrag mit
  Auflösungs-Trigger an und löst einen ab), `hard-rule-37-ohne-sensor` (2×, offen;
  der Slice begründet zweimal — §1 und Lerneintrag — mit §3.7, warum kein Sensor
  entsteht, was das dritte Vorkommen der Klasse wäre),
  `chronik-in-gelesenen-dateien` (2×, offen; sein `state.md` nennt namentlich
  `conventions.md`, die Datei, die dieser Slice ändert) und
  `rueckbau-eintrag-ablage-auslegung` (1×, offen; Gegenstand ist die Ablage eines
  Eintrags in `conventions/done/`, die dieser Slice vollzieht).
- `verifizierbar`: ja —
  `ls -d docs/plan/planning/observations/BEO-HARNESS/*/ | wc -l` → **14**;
  `grep -l 'Stand:\*\* offen' docs/plan/planning/observations/BEO-HARNESS/*/state.md | wc -l`
  → **10** (die übrigen vier tragen einen Ausgang);
  `git ls-tree -r --name-only d294a72 docs/plan/planning/observations/BEO-HARNESS/ | grep -c 'observation.md$'`
  → **14**. Kein Gate deckt es: `make verify-observations` prüft Deckung in der
  Gegenrichtung (zitierter Pfad hat ein Verzeichnis), nicht Vollständigkeit der
  Sichtung.
- `klasse`: Sichtungs-Schritt auf die vorab vermuteten Einträge verkürzt; die
  Negativ-Aussage reicht weiter als die Sichtung

### F-3 — `MR-021` nennt zu keiner seiner vier Zahlen den Geltungsbereich

- `kategorie`: MEDIUM
- `quelle`: `AGENTS.md` §5 §Geltungsbereich einer Messung (`seit slice-179`):
  „Wer eine Messung als **Beleg** schreibt … nennt ihren **Geltungsbereich** und
  sagt, ob er den Gegenstand deckt."
- `pfad`: `harness/conventions/MR-021-verfeinerungs-form.md:18–19`, `:21–22`, `:27–29`, `:31`
- `befund`: Die vier Zahlen (7 · 0 · 40 · 1) stehen ohne Angabe, worüber gemessen
  wurde — welche Dateimenge, welches Muster, ob der ADR-Index mitzählt, ob
  `Superseded`-ADRs mitzählen. Der Eintrag ist immutabel und soll laut
  Closure-Notiz gerade denjenigen tragen, der ihn „in einem Jahr prüft"; ohne
  Geltungsbereich muss der die Messung neu erfinden. Genau an dieser Stelle bricht
  F-1: Zwei der vier Zahlen (7, 1) sind gegen ihre Menge korrekt, eine (0) auch,
  und die vierte redet über eine andere Menge, als sie gezählt hat.
- `verifizierbar`: nein — ob ein Geltungsbereich genannt und weit genug ist, ist
  ein Urteil (`AGENTS.md` §3.7: die Regel hat ausdrücklich keinen Sensor).
- `klasse`: Beleg ohne Geltungsbereich in einem einfrierenden Artefakt

### F-4 — Risiko-Ausgang *weiter offen* → Register, ohne dass das Register etwas aufnimmt

- `kategorie`: MEDIUM
- `quelle`: Baseline `v6.5.0` · `regelwerk/modul-05-planning-harness.md`
  §Offene Risiken werden bei Closure aufgelöst („*weiter offen* → wandert ins
  **Beobachtungs-Register** … Der dritte Ausgang hängt das Risiko an den Zähler");
  `regelwerk/modul-06-roadmap.md` §Das Beobachtungs-Register
- `pfad`: `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:218–228`
  (§7 Risiko 2) gegen `:261–262` (§8, „kein neuer Eintrag, kein neuer Beleg")
- `befund`: Das zweite Risiko trägt den Ausgang *weiter offen* mit Ziel
  `BEO-HARNESS/zwei-baseline-staende-nach-migrationsende`. Dessen `state.md` steht
  auf **verkörpert** (`seit slice-176`), sein `evidence/` enthält genau eine Datei
  (`slice-170.md`), und §8 desselben Slice-Plans sagt ausdrücklich, dass kein
  Beleg angelegt wird. Damit zeigt der Ausgang auf einen Eintrag, der die
  Schwelle bereits verlassen hat, und der Zähler, an den der dritte Ausgang das
  Risiko hängen soll, bewegt sich nicht. Der Slice-Plan formuliert das Risiko
  zugleich als noch offene Querschnitts-Frage („Was beim nächsten Sprung mit ihnen
  geschieht, ist weiterhin nicht querschnittlich entschieden") — die im
  verkörperten Eintrag unter „Was nicht gelöst ist" steht, also außerhalb dessen,
  was der Zähler führt.
- `verifizierbar`: teilweise — `make verify-risiko-ausgaenge` und
  `make verify-observations` sind grün (eigener Lauf, Exit 0): der erste prüft,
  *dass* ein Ausgang aus der Dreier-Menge dasteht, der zweite nur die Deckung
  (Verzeichnis existiert, `evidence/` nicht leer). Ob der Ausgang *trägt*, ist
  laut Regelwerk ausdrücklich Urteil.
- `klasse`: Ausgang formal gesetzt, Wirkung nicht eingetreten

### F-5 — Slice §3: „ihre sechs relativen Links" — es sind fünf

- `kategorie`: MEDIUM
- `quelle`: Reviewer-Skill §Klassifikation (nachweislich falsche
  Tatsachenbehauptung); dieselbe Regel, die der Slice als Lerneintrag schärft
- `pfad`: `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:160–164` (die Zahl steht in Zeile 162)
- `befund`: Der Umsetzungs-Abschnitt begründet die Zwei-Commit-Teilung mit „die
  Datei liegt eine Ebene tiefer, also brauchen ihre **sechs** relativen Links ein
  `../` mehr". Die bewegte Datei trägt **fünf** relative Links (dreimal
  `../../conventions.md#mr-004`, je einmal auf `spec/spezifikation.md` und auf den
  Baseline-Abschnitt), und der Bewegungs-Commit `015c025` ändert entsprechend
  fünf Zeilenpaare. Die Zahl steht im selben Dokument, dessen Lerneintrag lautet,
  eine Begründung nenne ihren Beleg „mit Zahl und Quelle".
- `verifizierbar`: ja —
  `grep -o '](\.\.[^)]*)' harness/conventions/done/MR-011-verfeinerungs-form.md | wc -l`
  → **5**; `git show --stat 015c025` weist für die Datei `10 +++++-----` aus,
  also fünf geänderte Zeilen.
- `klasse`: Zählfehler in der Selbstbeschreibung des Vorgehens

### F-6 — Der Prosa-Rahmen der geänderten Tabelle trägt zwei Aussagen, die gegen sie falsch sind

- `kategorie`: MEDIUM
- `quelle`: `harness/conventions.md` §Aktive Adaptionen, gegen die Tabelle
  darunter; Klasse identisch mit der, die §2 des Slice als Anlass benennt
- `pfad`: `harness/conventions.md:116–119` (Aussage 1) und `:112–114` (Aussage 2)
- `befund`: Unmittelbar über der Tabelle, in die dieser Slice eine Zeile einfügt
  und aus der er eine entfernt, stehen zwei Aussagen über eben diese Tabelle.
  (1) „Die Spalte *Ersetzt-Baseline-Regel* … steht hier **überall** auf `—`" —
  gemessen tragen **fünf** der sieben aktiven Zeilen (MR-012, MR-014, MR-015,
  MR-016 und die neu eingefügte MR-021) einen Baseline-Anker; nur MR-019 und
  MR-020 tragen `—`. Der Slice entkräftet die Aussage mit seiner eigenen
  Einfügung weiter, ohne sie anzufassen. (2) „Jede Zeile trägt **zwei** Anker …
  und daneben den **alten Überschriften-Slug**" — gemessen trägt **keine** der
  sieben aktiven Zeilen einen zweiten Anker. Beides ist Altbestand und von diesem
  Slice nicht erzeugt; §1 schließt es weder aus noch nennt §2 es unter den „zwei
  Aussagen im Konventions-Bestand".
- `verifizierbar`: ja — `sed -n '119,130p' harness/conventions.md` gegen den
  Einleitungstext `sed -n '108,119p' harness/conventions.md`; die Anker-Zählung
  über `grep -c 'a id="mr-' ` je Zeile.
- `klasse`: überholte Aussage im unmittelbaren Umfeld der Änderung, nicht gesehen

### F-7 — §9-Ausschluss von `adaption-korrigiert-repo-aussage` nennt nicht das Merkmal des Eintrags

- `kategorie`: LOW
- `quelle`: `docs/plan/planning/observations/BEO-HARNESS/adaption-korrigiert-repo-aussage/observation.md`
- `pfad`: `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:287–294`
- `befund`: Der Registereintrag beschreibt eine Adaption, die „eine Repo-Aussage
  statt einer Baseline-Regel" korrigiert und „damit unter dem Fork-Test ein
  Rückbau-Kandidat" ist — das Merkmal ist der **fehlende** Baseline-Regel-Ersatz,
  so wie es die Tabellenzeile zu MR-020 ausdrücklich führt. `MR-021` trägt ein
  gefülltes `Ersetzt-Baseline-Regel`-Feld (`grundlagen-source-precedence.md`
  §ID-Schema als Klammer) und ist damit kein Kandidat; der Ausschluss trägt also
  im Ergebnis. Die im Slice gegebene Begründung nennt statt dieses Merkmals eine
  feinere Unterscheidung („korrigiert die **eigene Begründung** … und keine
  Repo-Aussage daneben"), die im Registereintrag nicht vorkommt. Der Slice
  schreibt dazu, die Unterscheidung stehe da, „damit ein späterer Leser sie
  nachrechnen kann" — nachrechnen lässt sie sich am Eintrag nicht.
- `verifizierbar`: nein — Urteil über die Zuordnung einer Beobachtung.
- `klasse`: tragende Begründung vorhanden, aber nicht die genannte

### F-8 — `015c025` führt `git mv` und Inhaltsänderung in einem Commit

- `kategorie`: LOW
- `quelle`: `AGENTS.md` §3.3 („(1) `git mv` als eigener Commit …, (2) Inhalt
  umschreiben als zweiter Commit")
- `pfad`: Commit `015c025` (`harness/conventions/MR-011-verfeinerungs-form.md` →
  `harness/conventions/done/…`, plus fünf geänderte Zeilen in derselben Datei)
- `befund`: Die Regel verlangt dem Buchstaben nach zwei Commits; der Gegenstand
  legt Bewegung und Pfadanpassung in einen. Die Präzedenz im Repo trennt beides:
  bei `MR-018` (slice-170) trug `f171af7` die Bewegung und `ceff549` die internen
  Links, bei `MR-017` (slice-166) ebenso. Der **Zweck** der Regel ist hier
  gemessen erfüllt — `git diff --name-status -M` weist `R072` aus, deutlich über
  der 50-%-Schwelle, `git log --follow` bleibt zuverlässig —, und der Slice-Plan
  weist diese Zahl selbst aus. Die Abweichung ist damit benannt, aber nicht als
  Abweichung: §3 stellt die Aufteilung als Erfüllung von §3.3 dar.
- `verifizierbar`: ja — `git diff --name-status -M 015c025^ 015c025` → `R072`.
- `klasse`: Zweck der Hard Rule erfüllt, Buchstabe gedehnt, Präzedenz abweichend

### F-9 — `MR-021`: das Lastenheft-Vorkommen zeigt nicht in die behauptete Richtung

- `kategorie`: LOW
- `quelle`: `spec/lastenheft.md:204`
- `pfad`: `harness/conventions/MR-021-verfeinerungs-form.md:29`
- `befund`: Der Satz lautet „Das Feld lebt in der **Gegenrichtung** — in 40
  ADR-Dateien, die auf `SPEC-*` zeigen, und einmal im Lastenheft." Das eine
  Lastenheft-Vorkommen ist „**Schärft:** [AC-FA-RULE-001] (`core-impurity`)" und
  zeigt damit `AC-*` → `AC-*` innerhalb des Vertrags-Stratums, nicht auf einen
  `SPEC-*`-Anker; es ist kein Beleg für die behauptete Gegenrichtung. Der
  Slice-Plan §2.1 unterscheidet das ausdrücklich („ADR → `SPEC-*` (bzw. im
  Lastenheft `AC-*` → `AC-*`)"), der immutable Eintrag übernimmt die
  Unterscheidung nicht.
- `verifizierbar`: ja — `grep -n 'Schärft' spec/lastenheft.md` → genau ein
  Treffer, Ziel `AC-FA-RULE-001`.
- `klasse`: Präzision beim Übertrag Plan → Eintrag verloren

### F-10 — DoD-Punkt 2 abgehakt, erfüllt hat ihn slice-182

- `kategorie`: INFO
- `quelle`: `AGENTS.md` §5 §Slice-Form (Liefer-Punkte); Baseline `v6.5.0` ·
  `regelwerk/modul-05-planning-harness.md` §Ziel-Form: Slice
- `pfad`: `docs/plan/planning/in-progress/slice-171-mr011-begruendung-und-rollen-tabelle.md:186–189`
  (DoD) und `:101–111` (§2.2)
- `befund`: Der Zustand ist erfüllt — `harness/README.md` nennt `MR-009` null Mal
  und trägt den Kontext-Trennungs-Absatz nicht mehr; die Rollen-Tabelle nennt
  `MR-016`. Der Slice misst das vor dem Abhaken und legt die Fremdherkunft offen
  („beides mit slice-182 erledigt"). Die Zustands-Lesart eines DoD-Punkts trägt.
  Beobachtbare Folge: slice-171 liefert damit **einen** eigenen Liefer-Punkt,
  während §1 seine Abgrenzung weiterhin gegen „vier Liefer-Punkte statt zwei"
  führt.
- `verifizierbar`: ja für den Zustand (`grep -c 'MR-009' harness/README.md` → 0);
  nein für die Zuordnung, wer ihn herstellte.
- `klasse`: fremd erfüllter DoD-Punkt, offengelegt

## Negativbefunde

- geprüft, ohne Befund: **Ablöse-Disziplin.** `015c025` ändert an `MR-011`
  ausschließlich fünf relative Link-Pfade, die der Umzug erzwingt (`../` → `../../`
  bzw. `../../` → `../../../`); Wortlaut von Adaption, Begründung,
  Auflösungs-Trigger und „Löst auf" sind zeichengleich. `git show 015c025 -M`
  zeigt keine inhaltliche Änderung. Der Eintrag behält `Status: Accepted` —
  konsistent mit `MR-010`, `MR-013`, `MR-017`, `MR-018` in `conventions/done/`.
- geprüft, ohne Befund: **Anker-Erhalt.** `mr-011` und `mr-021` lösen beide in
  `harness/conventions.md` auf (Zeile 150 bzw. 129); `make doc-check` grün. Beide
  Tabellen sind numerisch sortiert und decken sich mit den Dateien: sieben Zeilen
  gegen sieben Dateien in `conventions/`, vierzehn gegen vierzehn in
  `conventions/done/`.
- geprüft, ohne Befund: **kein lebender Verweis auf `MR-011` als aktiven
  Eintrag.** Alle verbleibenden Nennungen liegen in eingefrorenen Artefakten
  (`done/`-Slices, `evidence/`-Dateien, Review-Reports) oder im Gegenstand selbst;
  keine bezeichnet ihn als aktiv. Der alte Dateipfad `conventions/MR-011-…` kommt
  nur in einem Report von 2026-09-06 vor, der als Lauf-Beleg nicht editiert wird.
- geprüft, ohne Befund: **Grund 1 von `MR-021` trägt in der Sache.** Alle sieben
  `SPEC-*`-Überschriften in `spec/spezifikation.md` tragen **keinen** expliziten
  `<a id>` (gemessen: 0 Vorkommen von `<a id=` in der Datei); die ADR-Verweise
  hängen damit an generierten Überschriften-Slugs, und 29 `Accepted`-ADRs zeigen
  mit ihrem `Schärft:`-Feld darauf. Eine Umbenennung bräche sie, und der Nachzug
  in einer `Accepted`-ADR verstieße gegen `AGENTS.md` §3.5. Der Grund ist richtig
  — falsch ist nur die Zahl daneben (F-1).
- geprüft, ohne Befund: **die drei übrigen Zahlen in `MR-021`.** Sieben
  `SPEC-*`-Abschnitte in `spec/spezifikation.md` (`grep -c '^#\+.*SPEC-'` → 7,
  eindeutige Kennungen ebenfalls 7); **null** davon mit `Schärft:`-Feld; die
  Verfeinerung steht in allen sieben als Prosasatz „Präzisiert [`AC-…`]"; genau
  **ein** Vorkommen des Worts in der Datei, in Zeile 538 im Kopf der
  Historie-Sektion. Das dort zitierte „welche ADR eine Festlegung schärft,
  deklariert **die ADR** aufwärts in ihrem `Schärft:`-Feld" ist wortgleich
  wiedergegeben.
- geprüft, ohne Befund: **`AGENTS.md` §3.5.** Der Gegenstand fasst keine ADR an
  (`git show --name-only` über alle vier Commits: nur `harness/` und
  `docs/plan/planning/`); `make doc-immutable` läuft in `make gates` grün.
- geprüft, ohne Befund: **`AGENTS.md` §3.7 in `MR-021`.** Der Block „Was dieser
  Eintrag gegenüber `MR-011` korrigiert" beschreibt zwar abwesenden Text, ist
  aber die von der Adaptions-Disziplin verlangte Form („Korrekturen entstehen als
  neuer `MR` oder als ausdrückliche Aufhebung") und steht in einem
  Entscheidungs-Artefakt, nicht in einem Kommentar oder Zustandsfeld. Kein
  Konjunktiv über Verworfenes; die Aussagen stehen im Indikativ über den Zustand.
- geprüft, ohne Befund: **`AGENTS.md` §5 Commit-Disziplin.** Alle vier Commits
  nennen `slice-171`. Scope-Trennung sauber: `docs(planning)` berührt
  ausschließlich `docs/plan/planning/`, `docs(harness)` ausschließlich `harness/`;
  `make commit-scope-check RANGE=eb79a60..1f6aed7` Exit 0.
- geprüft, ohne Befund: **Slice-Form.** Kopf trägt `Verantwortlich:`, `Autor:`,
  `Welle:`, `Berührte Spec-Stellen:`; §1 heißt *Ziel und Abgrenzung* und führt
  drei Ausschlüsse je mit Begründung; §8 trägt die Zeile `Lerneintrag — Form:
  geschärfte Regel`; §9 heißt *Sub-Area-Prüfungen und Modus-Begründung* und führt
  beide Vorgelagert-Blöcke; der Gate-Lauf steht als feste Zeile statt als
  Checkbox. DoD zählt nach `tasks-ignore-pattern` zwei Punkte (≤ 3).
  `make doc-structure` grün.
- geprüft, ohne Befund: **WIP-Limit.** `docs/plan/planning/in-progress/` trägt
  außer `roadmap.md` genau einen Slice.
- geprüft, ohne Befund: **Gate-Behauptungen.** Die Commit-Messages behaupten
  „`make gates`: Exit 0" bzw. „`make gates` / `make verify`: Exit 0". Eigene
  Läufe auf dem Stand `1f6aed7`: `make gates` Exit 0, `make verify` Exit 0
  (21 Anforderungen, 0 Waisen).
- geprüft, ohne Befund: **`v6.0.0`-Aussage in §7/§9.** Der Ausgang korrigiert die
  Plan-Aussage zu Recht: alle **fünf** `Ersetzt-Baseline-Regel`-Felder aktiver
  Einträge zeigen auf `v6.5.0`, keines auf einen älteren Stand — gemessen sowohl
  in den `MR`-Dateien als auch in der Tabellenspalte.
- geprüft, ohne Befund: **§2.3 „48 Einträge".** Bei Plan-Anlage (`d294a72`) trug
  das Register 48 `observation.md`-Dateien; heute sind es 54. Die Zahl hält
  korrekt ihren Stand fest.
- geprüft, ohne Befund: **`MR-021`-Pflichtfelder.** Status, Datum,
  Geltungsbereich, `Ersetzt-Baseline-Regel`, Adaption, Begründung,
  Auflösungs-Trigger, „Löst ab", „Ausgelöst durch Baseline-Stand" — vollständig
  nach der Adaptions-Block-Disziplin; die Kennung `MR-021` schließt lückenlos an
  `MR-020` an.

## Kategorie-Summary

| Kategorie | Anzahl | Findings |
|---|---|---|
| HIGH | 2 | F-1, F-2 |
| MEDIUM | 4 | F-3, F-4, F-5, F-6 |
| LOW | 3 | F-7, F-8, F-9 |
| INFO | 1 | F-10 |

## Verdikt

**Nicht abschlussbereit.**

Die Entscheidung des Slice ist richtig und sein Weg sauber: `MR-011` wurde nicht
überschrieben, sondern bewegt und abgelöst, beide Anker lösen auf, die Commits
sind scope-rein, `make gates` und `make verify` sind grün. Der **tragende Grund**
von `MR-021` — `SPEC-*`-Anker hängen an generierten Überschriften-Slugs, auf die
29 `Accepted`-ADRs zeigen — hält der Nachprüfung stand.

Blockierend sind die zwei HIGH, und beide liegen in derselben Klasse, gegen die
der Slice angetreten ist. F-1: Der Eintrag, der eine ungemessene Begründung durch
eine gemessene ersetzt, trägt selbst eine Zahl, die über eine andere Menge zählt
als die, über die sein Satz aussagt — und er ist immutabel, also ist die Zahl
nicht korrigierbar, sondern nur ablösbar. F-2: Der Sichtungs-Schritt wurde auf die
im Plan vorab vermuteten zwei Einträge verkürzt und schließt mit einer
Negativ-Aussage über die ganze Sub-Area, die gegen das Register nicht hält;
darunter liegt mit `hard-rule-37-ohne-sensor` ein Eintrag bei 2×, den dieser Slice
zweimal berührt.

F-3 ist die Klammer um beide: Hätte der Eintrag zu jeder Zahl ihren
Geltungsbereich genannt — die Regel steht seit slice-179 in `AGENTS.md` §5 —,
wäre F-1 beim Schreiben aufgefallen. Die MEDIUM-Findings F-4 bis F-6 sind
unabhängig davon zu entscheiden; F-6 betrifft Altbestand, den dieser Slice nicht
erzeugt, aber beim Anfassen derselben Tabelle passiert hat.
